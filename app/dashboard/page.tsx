"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export default function Dashboard(){const router=useRouter();const [user,setUser]=useState<any>(null);const [amount,setAmount]=useState(1000);const [error,setError]=useState("");const [loading,setLoading]=useState(false);
useEffect(()=>{fetch("/api/me").then(async r=>{if(!r.ok){router.replace("/");return}const d=await r.json();setUser(d.user)}).catch(()=>router.replace("/"))},[router]);
async function recharge(){setError("");setLoading(true);try{const r=await fetch("/api/payments/initiate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({amount})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Erreur");alert(d.message||"Paiement initialisé");}catch(e){setError(e instanceof Error?e.message:"Erreur serveur.")}finally{setLoading(false)}}
async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.replace("/");router.refresh()}
if(!user)return <div className="dash">Chargement...</div>;return <><nav className="nav"><div className="logo">Mon<span>Compte</span></div><button className="linkbtn" onClick={logout}>Se déconnecter</button></nav><main className="dash"><h1>Bonjour {user.name} 👋</h1><div className="balance"><div>Solde disponible</div><div className="amount">{user.balance.toLocaleString("fr-FR")} XOF</div></div><div className="grid"><section className="panel"><h3>Recharger</h3>{error&&<div className="error">{error}</div>}<div className="field"><label>Montant (XOF)</label><input type="number" min="100" step="100" value={amount} onChange={e=>setAmount(Number(e.target.value))}/></div><button className="primary" onClick={recharge} disabled={loading}>{loading?"Préparation...":"Continuer vers le paiement"}</button></section><section className="panel"><h3>Mon compte</h3><p><b>Téléphone :</b> {user.phone}</p><p className="small">Le crédit de solde doit être confirmé côté serveur après notification du prestataire de paiement.</p></section></div></main></>}
